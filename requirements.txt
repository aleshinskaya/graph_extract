
#nvm
https://github.com/nvm-sh/nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

#node
nvm install node

#vis-network
npm install vis-network

#python modules
os, sys, math, requests, json, jsonlines, pandas, numpy, re, textwrap, dotenv, typer, importlib, requests, re, numpy, pandas, dotenv
